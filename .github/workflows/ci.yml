name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  rust:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Read Rust toolchain pin
        id: rust_toolchain
        run: |
          channel=$(sed -n 's/^channel = "\(.*\)"/\1/p' rust-toolchain.toml)
          if [[ -z "$channel" ]]; then
            echo "missing channel in rust-toolchain.toml" >&2
            exit 1
          fi
          echo "channel=${channel}" >> "$GITHUB_OUTPUT"
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ steps.rust_toolchain.outputs.channel }}
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential pkg-config \
            libsqlite3-dev
      - run: make ci

  x11-check:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Read Rust toolchain pin
        id: rust_toolchain
        run: |
          channel=$(sed -n 's/^channel = "\(.*\)"/\1/p' rust-toolchain.toml)
          if [[ -z "$channel" ]]; then
            echo "missing channel in rust-toolchain.toml" >&2
            exit 1
          fi
          echo "channel=${channel}" >> "$GITHUB_OUTPUT"
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ steps.rust_toolchain.outputs.channel }}
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - name: Install X11 development packages
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential pkg-config \
            libx11-dev libxrandr-dev
      - name: Check lock-x11 with the x11 feature
        run: |
          cargo check -p recallgate-lock-x11 --features x11 --all-targets
          cargo clippy -p recallgate-lock-x11 --features x11 --all-targets -- -D warnings
